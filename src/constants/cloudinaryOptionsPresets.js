const USER_AVATAR = {
  use_filename: true,
  unique_filename: false,
  overwrite: true,
  folder: 'petly_api/user_avatars',
};

const OWN_AVATAR = {
  use_filename: true,
  unique_filename: false,
  overwrite: true,
  folder: 'petly_api/own_avatars',
};

const NOTICE_AVATAR = {
  use_filename: true,
  unique_filename: false,
  overwrite: true,
  folder: 'petly_api/notice_avatars',
};

module.exports = { USER_AVATAR, OWN_AVATAR, NOTICE_AVATAR };
